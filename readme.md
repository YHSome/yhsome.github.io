ai助手你好：

我刚刚为你做好了一个博客渲染器的框架，但是目前还没有具体落地的代码；

你需要做的事：

    1.在动手前先提交一次git，即将当前空白项目提交至git
    2.将/OpenBlogger/template/default中的所有html文件按照我在html内提的要求完成该html的代码书写（风格不限，但要求风格统一）
    3.写一个python代码，代码文件存放在./OpenBlogger中，功能：将./Raw中的所有**未渲染**的md文件按照用户指定的模板进行渲染（默认为default,即对应./Template/Default），将输出的文件放置在Rendered文件夹中，同时，对于格式不规范的md文件，补全渲染的日期（当前时间）与标题（默认第一句，字数超出20字后用省略号）

注意：你所写的所有python代码文件要放在./OpenBlogger里面，以保证根目录的整洁性